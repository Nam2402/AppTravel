<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nam.travel">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".views.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
         <activity android:name=".MainActivity" />
        <activity android:name=".views.signup.SignupActivity" />
        <!--<activity android:name=".views.login.LoginActivity" />-->
        <activity android:name=".views.place.PlaceActivity" />
        <activity android:name=".views.myProfile.EditProfileActivity" />
        <activity android:name=".views.location.detailLocation.DetailLocationActivity" />
        <activity android:name=".views.location.allLocation.AllLocationActivity" />
        <activity android:name=".views.review.CommentActivity" />
        <activity android:name=".views.note.NoteActivity" />
        <activity android:name=".views.location.detailLocation.LoveLocationActivity"></activity>
    </application>

</manifest>